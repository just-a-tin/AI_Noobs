{
  "name": "sentinel-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Sentinel MV3 extension. DORMANT: the extension currently loads unpacked with no build step. This config is here for the TypeScript migration once Node is installed.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
